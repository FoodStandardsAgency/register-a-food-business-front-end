describe("Edit controller", () => {
  describe("given an answer with white space", () => {
    it("should return the answer without whitespace", () => {});
  });
  describe("given an answer on this page was previously truthy and is now not part of the new answers", () => {
    it("should not be part of the returned cumulative answers", () => {});
  });
  describe("given previous answers and new answers", () => {
    it("should return cumulative answers that include the new answers", () => {});
  });
  describe("given that there are validation errors", () => {
    it("should return the redirect route", () => {});
    it("should return the errors", () => {});
  });
  describe("given that the cumulative edit route answers are associated with switches", () => {
    describe("given that the current page is at the end of the edit route", () => {});
    describe("given that the current page is NOT at the end of the edit route", () => {});
    // TESTS FOR EDIT PATH FUNCTION
    // describe("given that the new answers trigger the same switches as the old answers", () => {
    //   it("should return a redirect route with the next page", () => {});
    // });
    // describe("given that the new answers trigger different switches to the old answers", () => {
    //     it("should ", () => {});
    //   });
  });
  describe("given that the cumulative edit route answers are NOT associated with switches", () => {
    describe("given", () => {
      it("should ", () => {});
    });
  });
});

describe("given", () => {
  it("should ", () => {});
});
